# Netlify Runtime Configuration
# This file provides additional runtime configuration to ensure root deployment

[build]
base = ""
publish = "."
functions = "netlify/functions"

[dev]
framework = "#static"
targetPort = 8080
publish = "."
